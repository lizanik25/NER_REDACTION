services:
  ner-redaction-api:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - ./models/final_model:/app/models/final_model
      - ./outputs:/app/outputs
    environment:
      - PYTHONPATH=/app
